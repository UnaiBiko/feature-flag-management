const createConfigFeatureFlags = require("./createConfigFeatureFlags");
const featureFlagService = require("./featureFlagService");

module.exports = {
  createConfigFeatureFlags,
  featureFlagService,
};
